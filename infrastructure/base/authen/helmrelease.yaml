---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: internal-authen
  namespace: flux-system
spec:
  chart:
    spec:
      chart: keycloax
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: codecentric
      version: 7.1.5
  interval: 30m0s
