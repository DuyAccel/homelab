---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openebs
spec:
  chart:
    spec:
      chart: openebs
      version: "4.4.0"
      sourceRef:
        kind: HelmRepository
        name: openebs
        namespace: flux-system

  interval: 72h
  valuesFrom: []
