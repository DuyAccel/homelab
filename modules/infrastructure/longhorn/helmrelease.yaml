---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
spec:
  chartRef:
    kind: HelmRepository
    name: longhorn
    namespace: flux-system
  interval: 72h
  valuesFrom: []
