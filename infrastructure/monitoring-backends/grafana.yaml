---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana
  namespace: observability-stack
spec:
  interval: 10m
  chart:
    spec:
      chart: grafana
      version: ">= 8.0.0"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  values:
    persistence:
      enabled: true
      storageClassName: longhorn
      size: 5Gi
    datasources:
      datasources.yaml:
        apiVersion: 1
        datasources:
          - name: VictoriaMetrics
            type: prometheus
            url: http://victoria-metrics-victoria-metrics-single.observability-stack.svc.cluster.local:8428
            access: proxy
            isDefault: true
          - name: Loki
            type: loki
            url: http://loki.observability-stack.svc.cluster.local:3100
            access: proxy
          - name: Tempo
            type: tempo
            url: http://tempo.observability-stack.svc.cluster.local:3100
            access: proxy
            jsonData:
              httpMethod: GET
              tracesToLogsV2:
                datasourceUid: "Loki"
                spanStartTimeShift: "-1h"
                spanEndTimeShift: "1h"
                tags:
                  [
                    { key: "service.name", value: "service" },
                    { key: "host.name", value: "host" },
                  ]
                filterByTraceID: true
                filterBySpanID: true
              tracesToMetrics:
                datasourceUid: "VictoriaMetrics"
                spanStartTimeShift: "-1h"
                spanEndTimeShift: "1h"
                tags:
                  [
                    { key: "service.name", value: "service" },
                    { key: "host.name", value: "host" },
                  ]
              serviceMap:
                datasourceUid: "VictoriaMetrics"
