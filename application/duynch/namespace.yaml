apiVersion: v1
kind: Namespace
metadata:
  name: duynch
  labels:
    networking-zone: external
    # --- Istio Ambient Mesh Options ---
    # Enrolls this namespace into the Ambient Mesh (ztunnel L4 routing/mTLS)
    # istio.io/dataplane-mode: ambient
    #
    # Binds the namespace to use the waypoint proxy for L7 rules
    # istio.io/use-waypoint: waypoint
